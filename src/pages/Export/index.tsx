import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const ExportPage: React.FC = () => {
  return (
    <div>
      <Title level={2}>Export Center</Title>
      <p>Data export tools coming soon...</p>
    </div>
  );
};

export default ExportPage;