import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const SchedulerPage: React.FC = () => {
  return (
    <div>
      <Title level={2}>Meeting Scheduler</Title>
      <p>Meeting scheduler coming soon...</p>
    </div>
  );
};

export default SchedulerPage;